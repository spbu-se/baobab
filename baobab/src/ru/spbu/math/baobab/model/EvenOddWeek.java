package ru.spbu.math.baobab.model;

/**
 * enum to stay for all / odd (numerator in spreadsheet) / even (denominator in spreadsheet) weeks
 * 
 * @author dluciv
 */
public enum EvenOddWeek {
  ALL,  // the both
  ODD,  // numerator
  EVEN  // denominator
}
